# Example .env for dockerized deployment
# Copy to .env and fill values before running `docker compose up`

# Django
DJANGO_DEBUG=False
DJANGO_SECRET=replace-with-a-long-random-secret
DJANGO_SETTINGS_MODULE=soundbox.settings
ALLOWED_HOSTS=141.11.123.238,your.domain.com

# NOTE: This repository's compose file can be configured to use hard-coded
# values inside `docker-compose.yml`. If you switch to that mode you do
# NOT need this file. Keeping DB credentials in source is insecure for
# production — this example remains for local/development usage only.
DB_NAME=soundbox_db
DB_USER=soundbox_user
DB_PASSWORD=your_db_password
DB_HOST=localhost
DB_PORT=5432
